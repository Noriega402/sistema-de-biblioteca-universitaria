
// Config para JWT por Authorization header (sin cookie HttpOnly)
export const API_BASE = "http://localhost:8085"; // sin /api, de acuerdo a tu gateway actual
export const USE_HTTPONLY_COOKIE = false;        // << clave: false para usar Authorization: Bearer
